# Cursor / AI 에이전트 — 이 프로젝트에서 가장 먼저

작업 전 **아래 문서 순서**를 기본 맥락으로 삼을 것.

| 순서 | 경로 |
|------|------|
| 1 | 루트 **`README.md`** — Cursor 자동 표시·GitHub 흐름·코드 구조 요약 |
| 2 | `Assets/TeamDistribution/Docs/BEGINNER_CURSOR_KR.md` |
| 3 | `Assets/TeamDistribution/Docs/TEAM_BUILD.md` |

- **Unity**: `ProjectSettings/ProjectVersion.txt`와 맞출 것 (README에 LTS 안내)  
- **팀 코어**: `Assets/TeamDistribution/` — `Scripts/` / `Demo/` / `Editor/` / `Docs/`  
- **규칙 상세**: `.cursor/rules/read-docs-first.mdc` — 매 채팅 자동 적용 아님. 필요 시 `@read-docs-first` 로 불러오기. 사람용 첫 안내는 워크스페이스 열 때 `README.md` 자동 표시(`.vscode/settings.json`).

**Git / GitHub (비공개 팀)**  
- 코드·에셋 제안 시 `Library/`·`Temp/`·`Logs/`·`UserSettings/`를 건드리거나 커밋 대상으로 취급하지 말 것.  
- 팀원 워크플로: 작업 전 pull, 저장 후 push — **`README.md`의「GitHub (비공개 팀)」** 절. **첫 원격 푸시**는 같은 문서 **「최초 푸시 체크리스트」** 를 따를 것.

Unity·Git·Cursor를 처음 쓰는 팀원 전제.
